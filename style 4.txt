/* ============================================
   CARD QUEST — All Device Responsive
   Mobile / Tablet / Laptop / Desktop
   ============================================ */

:root {
  --bg-dark:        #07071a;
  --bg-card:        #131330;
  --bg-card-hover:  #1e1e45;
  --gold:           #ffd700;
  --cyan:           #00d4ff;
  --purple:         #a855f7;
  --hp-green:       #00ff88;
  --hp-red:         #ff3344;
  --text-primary:   #e2e8f0;
  --text-secondary: #7a8599;
  --correct:        #22c55e;
  --wrong:          #ef4444;
  --border:         #252550;
  --card-border:    #3a3a7a;
  --sat: env(safe-area-inset-top,    0px);
  --sab: env(safe-area-inset-bottom, 0px);
  --sal: env(safe-area-inset-left,   0px);
  --sar: env(safe-area-inset-right,  0px);
  --arena-h:      140px;
  --slot-h:        72px;
  --qp-q-fs:       1rem;
  --qp-opt-fs:     0.88rem;
  --qp-opt-minh:   44px;
  --qp-body-maxh:  clamp(200px, 42vh, 360px);
}

*, *::before, *::after {
  margin: 0; padding: 0; box-sizing: border-box;
}
html {
  height: 100%;
  overflow: hidden;
  overscroll-behavior: none;
}
body {
  height: 100%;
  overflow: hidden;
  font-family: 'Chakra Petch', 'Segoe UI', sans-serif;
  background: #000;
  color: var(--text-primary);
  -webkit-user-select: none;
  user-select: none;
  position: fixed;
  width: 100%;
  display: flex;
  justify-content: center;
  align-items: center;
}

/* ── Root wrap ── */
#root-wrap {
  width: 100%; height: 100%;
  display: flex;
  justify-content: center;
  align-items: center;
  background:
    radial-gradient(ellipse at 20% 50%,
      rgba(168,85,247,0.06), transparent 55%),
    radial-gradient(ellipse at 80% 30%,
      rgba(0,212,255,0.05), transparent 50%),
    #000;
}

/* ── Game Container ── */
#game-container {
  width: 100%;
  max-width: 520px;
  height: 100vh;
  height: 100dvh;
  position: relative;
  overflow: hidden;
  background: var(--bg-dark);
  display: flex;
  flex-direction: column;
  box-shadow: 0 0 60px rgba(0,0,0,0.9);
  padding-top:    var(--sat);
  padding-bottom: var(--sab);
  padding-left:   var(--sal);
  padding-right:  var(--sar);
}

/* ── Screens ── */
.screen {
  position: absolute;
  inset: 0;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: flex-start;
  z-index: 1;
}
.hidden { display: none !important; }

/* ════════════════════════════════
   LOGIN
   ════════════════════════════════ */
#login-screen {
  overflow-y: auto;
  -webkit-overflow-scrolling: touch;
  justify-content: center;
  background:
    radial-gradient(ellipse at 30% 40%,
      rgba(168,85,247,0.08), transparent 55%),
    var(--bg-dark);
}
.login-box {
  text-align: center;
  padding: clamp(1.2rem,4vh,2.5rem)
           clamp(1.2rem,5vw,2rem);
  width: 92%; max-width: 440px;
}
.login-badge {
  font-size: clamp(2rem,6vw,3rem);
  margin-bottom: 0.4rem;
  filter: drop-shadow(0 0 14px rgba(255,215,0,0.5));
  display: block;
}
.login-title {
  font-family: 'Press Start 2P', monospace;
  font-size: clamp(1.1rem,4.5vw,1.9rem);
  color: var(--gold);
  text-shadow: 0 0 20px rgba(255,215,0,0.4), 0 2px 0 #8b6914;
  margin-bottom: 0.4rem;
  letter-spacing: 3px;
  animation: titlePulse 2s ease-in-out infinite alternate;
}
@keyframes titlePulse {
  from { text-shadow: 0 0 10px rgba(255,215,0,0.3), 0 2px 0 #8b6914; }
  to   { text-shadow: 0 0 35px rgba(255,215,0,0.7),
                      0 0 70px rgba(255,215,0,0.2), 0 2px 0 #8b6914; }
}
.login-subtitle {
  color: var(--cyan);
  font-size: clamp(0.78rem,2.5vw,1rem);
  font-weight: 600;
  margin-bottom: 0.5rem;
}
.login-dragon {
  font-size: clamp(3.5rem,12vw,6rem);
  margin: clamp(0.4rem,1.5vh,0.9rem) 0;
  animation: dragonFloat 3s ease-in-out infinite;
  filter: drop-shadow(0 0 22px rgba(255,80,0,0.5));
  display: block;
}
@keyframes dragonFloat {
  0%,100% { transform: translateY(0)     rotate(-2deg); }
  50%     { transform: translateY(-16px) rotate(2deg);  }
}
.feature-pills {
  display: flex; flex-wrap: wrap;
  gap: 7px; justify-content: center;
  margin-bottom: 0.8rem;
}
.pill {
  background: rgba(255,215,0,0.1);
  border: 1px solid rgba(255,215,0,0.3);
  border-radius: 20px; padding: 4px 12px;
  font-size: clamp(0.65rem,1.9vw,0.8rem);
  color: var(--gold); font-weight: 600;
}
.login-hint {
  color: var(--text-secondary);
  font-size: clamp(0.75rem,2.2vw,0.9rem);
  margin-bottom: clamp(0.7rem,2vh,1.2rem);
  font-style: italic; line-height: 1.5;
}
#pin-input {
  width: 100%;
  padding: clamp(12px,2.2vh,17px) 18px;
  font-size: clamp(0.95rem,3vw,1.15rem);
  text-align: center;
  background: var(--bg-card);
  border: 2px solid var(--border);
  border-radius: 14px;
  color: var(--text-primary);
  font-family: 'Chakra Petch', sans-serif;
  outline: none;
  margin-bottom: clamp(0.7rem,1.8vh,1.2rem);
  transition: border-color 0.3s, box-shadow 0.3s;
  letter-spacing: 2px;
  -webkit-user-select: text;
  user-select: text;
}
#pin-input::placeholder {
  color: var(--text-secondary); letter-spacing: 0;
}
#pin-input:focus {
  border-color: var(--gold);
  box-shadow: 0 0 22px rgba(255,215,0,0.18);
}
#start-btn, #try-again-btn {
  width: 100%;
  padding: clamp(13px,2.2vh,18px);
  font-size: clamp(0.92rem,2.8vw,1.1rem);
  font-weight: 700;
  font-family: 'Chakra Petch', sans-serif;
  border: none; border-radius: 14px;
  cursor: pointer;
  transition: transform 0.15s, box-shadow 0.3s;
  letter-spacing: 1px;
  -webkit-tap-highlight-color: transparent;
  position: relative; overflow: hidden;
}
#start-btn {
  background: linear-gradient(135deg, #ffd700, #ff8c00);
  color: #1a0a00;
  box-shadow: 0 4px 22px rgba(255,140,0,0.38);
}
#start-btn::after {
  content: ''; position: absolute; inset: 0;
  background: linear-gradient(90deg,
    transparent, rgba(255,255,255,0.28), transparent);
  transform: translateX(-100%);
  animation: shimmer 2.5s ease-in-out infinite;
}
@keyframes shimmer {
  0%,100% { transform: translateX(-100%); }
  50%     { transform: translateX(100%);  }
}
#start-btn:hover  {
  transform: translateY(-2px);
  box-shadow: 0 6px 32px rgba(255,140,0,0.5);
}
#start-btn:active { transform: scale(0.98); }
#start-btn:disabled { opacity: 0.6; cursor: wait; }
#error-msg {
  color: var(--hp-red);
  margin-top: 0.9rem;
  font-size: clamp(0.75rem,2.2vw,0.9rem);
  font-weight: 600; line-height: 1.5; padding: 0 4px;
}
.login-footer {
  display: flex; justify-content: center;
  gap: 10px; margin-top: 1.1rem; flex-wrap: wrap;
}
.login-footer span {
  font-size: clamp(0.62rem,1.8vw,0.75rem);
  color: var(--text-secondary);
  background: var(--bg-card);
  border: 1px solid var(--border);
  border-radius: 8px; padding: 5px 12px;
}

/* ════════════════════════════════
   BATTLE SCREEN
   ════════════════════════════════ */
#battle-screen {
  justify-content: flex-start;
  overflow: hidden;
  position: absolute; inset: 0;
}

/* ── HUD ── */
#hud-top {
  display: flex; align-items: center;
  justify-content: space-between;
  padding: clamp(5px,1.1vh,9px)
           clamp(10px,2.5vw,18px)
           clamp(4px,0.9vh,7px);
  width: 100%; gap: clamp(5px,1.2vw,12px);
  background: rgba(0,0,0,0.55);
  border-bottom: 1px solid rgba(255,255,255,0.05);
  flex-shrink: 0;
}
.hp-section { flex: 1; min-width: 0; }
.hp-label {
  font-size: clamp(0.38rem,1.1vw,0.54rem);
  font-weight: 700; margin-bottom: 3px;
  color: var(--text-secondary);
  display: flex; align-items: center;
  justify-content: space-between;
  gap: 4px; letter-spacing: 0.5px;
}
.hp-bar {
  width: 100%;
  height: clamp(10px,1.9vh,16px);
  background: #0d0d20; border-radius: 8px;
  overflow: hidden; border: 1.5px solid var(--border);
}
.hp-fill {
  height: 100%; width: 100%; border-radius: 8px;
  transition: width 0.6s cubic-bezier(0.22,1,0.36,1);
  position: relative;
}
.hp-fill::after {
  content: ''; position: absolute;
  top: 2px; left: 4px; right: 4px; height: 2px;
  background: rgba(255,255,255,0.2); border-radius: 4px;
}
.enemy-fill  { background: linear-gradient(90deg,#cc2233,#ff5544); }
.player-fill { background: linear-gradient(90deg,#1d4ed8,var(--cyan)); }
#score-badge {
  text-align: center;
  min-width: clamp(50px,12vw,80px); flex-shrink: 0;
}
#score-label {
  font-size: clamp(0.28rem,0.85vw,0.42rem);
  font-family: 'Press Start 2P', monospace;
  color: var(--text-secondary); margin-bottom: 1px;
}
#score-display {
  font-family: 'Press Start 2P', monospace;
  font-size: clamp(0.6rem,1.9vw,0.88rem);
  color: var(--gold);
  text-shadow: 0 0 10px rgba(255,215,0,0.4);
}
#round-label {
  font-size: clamp(0.26rem,0.8vw,0.38rem);
  font-family: 'Press Start 2P', monospace;
  color: var(--purple); margin-top: 2px;
}

/* ── ARENA ── */
#arena {
  width: 100%; height: var(--arena-h);
  position: relative;
  display: flex; align-items: flex-end;
  justify-content: space-between;
  padding: 0 clamp(14px,5vw,36px)
             clamp(8px,1.8vh,16px);
  overflow: hidden; flex-shrink: 0;
}
#arena-bg {
  position: absolute; inset: 0;
  background: linear-gradient(180deg,
    #080820 0%,#100828 40%,#0c1a0c 80%,#0a140a 100%);
  z-index: 0;
}
#arena-bg::before {
  content: ''; position: absolute; inset: 0;
  background:
    radial-gradient(1.5px 1.5px at 15% 25%,rgba(255,255,255,0.5),transparent),
    radial-gradient(1px   1px   at 35% 65%,rgba(255,255,255,0.3),transparent),
    radial-gradient(2px   2px   at 55% 15%,rgba(200,200,255,0.6),transparent),
    radial-gradient(1px   1px   at 75% 55%,rgba(255,255,255,0.4),transparent),
    radial-gradient(1.5px 1.5px at 90% 35%,rgba(200,200,255,0.5),transparent),
    radial-gradient(1px   1px   at 8%  75%,rgba(255,220,150,0.4),transparent);
  animation: twinkle 5s ease-in-out infinite alternate;
}
@keyframes twinkle { from{opacity:0.4} to{opacity:1} }

#boss-wrap {
  position: relative; z-index: 2;
  display: flex; align-items: center; justify-content: center;
}
#boss-sprite {
  font-size: clamp(2rem,7vw,3.8rem);
  animation: bossIdle 2.5s ease-in-out infinite;
  filter: drop-shadow(0 0 18px rgba(255,50,50,0.5));
  position: relative; z-index: 2; line-height: 1;
}
@keyframes bossIdle {
  0%,100% { transform: translateY(0)     rotate(-3deg); }
  50%     { transform: translateY(-10px) rotate(3deg);  }
}
#boss-health-ring {
  position: absolute;
  width: clamp(65px,17vw,105px);
  height: clamp(65px,17vw,105px);
  top: 50%; left: 50%;
  transform: translate(-50%,-50%); z-index: 1;
}
#boss-ring-svg { width: 100%; height: 100%; }
#arena-center {
  position: absolute; left: 50%; top: 0;
  transform: translateX(-50%);
  z-index: 10; pointer-events: none;
  display: flex; flex-direction: column; align-items: center;
}
#player-wrap { position: relative; z-index: 2; }
#player-sprite {
  font-size: clamp(1.8rem,6.5vw,3.2rem);
  animation: playerIdle 2s ease-in-out infinite;
  filter: drop-shadow(0 6px 12px rgba(0,0,0,0.5));
  line-height: 1;
}
@keyframes playerIdle {
  0%,100% { transform: translateY(0);    }
  50%     { transform: translateY(-8px); }
}

/* Particles & floats */
#particles { position:absolute; inset:0; pointer-events:none; z-index:5; }
.particle {
  position: absolute; border-radius: 50%;
  animation: particleFly 0.7s ease-out forwards;
}
@keyframes particleFly {
  0%   { opacity:1; transform:translate(0,0) scale(1); }
  100% { opacity:0; transform:translate(var(--px),var(--py)) scale(0); }
}
.float-text {
  position: absolute; z-index: 10;
  font-family: 'Press Start 2P', monospace;
  pointer-events: none;
  text-shadow: 2px 2px 6px rgba(0,0,0,0.9);
  white-space: nowrap;
}
#damage-number {
  font-size: clamp(0.85rem,2.8vw,1.2rem);
  color: #ff4444; top: 10%; left: 50%;
  transform: translateX(-50%);
}
#effect-display {
  font-size: clamp(0.44rem,1.4vw,0.62rem);
  color: var(--gold); top: 40%; left: 50%;
  transform: translateX(-50%);
}
#heal-display {
  font-size: clamp(0.42rem,1.3vw,0.58rem);
  color: var(--hp-green); top: -10px; left: 50%;
  transform: translateX(-50%);
}
#explosion {
  position: absolute;
  font-size: clamp(2rem,7vw,3.5rem);
  z-index: 4; pointer-events: none;
}

/* Animations */
.anim-float-up {
  animation: floatUp 1.4s ease-out forwards !important;
}
@keyframes floatUp {
  0%   { opacity:1; transform:translateX(-50%) translateY(0)     scale(1);   }
  60%  { opacity:1; transform:translateX(-50%) translateY(-22px)  scale(1.1); }
  100% { opacity:0; transform:translateX(-50%) translateY(-46px)  scale(0.8); }
}
.flash-overlay {
  position: absolute; inset: 0; z-index: 20; pointer-events: none;
  animation: flashBang 0.35s ease-out forwards;
}
@keyframes flashBang {
  from { background: rgba(255,255,255,0.4); }
  to   { background: transparent; }
}
.anim-boss-hit { animation: bossHit 0.65s ease-out !important; }
@keyframes bossHit {
  0%   { filter:drop-shadow(0 0 18px rgba(255,50,50,0.5)); }
  15%  { filter:brightness(6) drop-shadow(0 0 30px #fff);
          transform:translateY(-12px) rotate(5deg) scale(1.18); }
  35%  { filter:brightness(3) drop-shadow(0 0 20px #ff4400);
          transform:translateY(6px) rotate(-4deg); }
  60%  { filter:brightness(1.5); transform:translateY(-3px); }
  100% { filter:drop-shadow(0 0 18px rgba(255,50,50,0.5));
          transform:translateY(0); }
}
.anim-player-hit { animation: playerHit 0.65s ease-out !important; }
@keyframes playerHit {
  0%   { filter:drop-shadow(0 6px 12px rgba(0,0,0,0.5)); }
  15%  { filter:brightness(5) drop-shadow(0 0 25px #fff);
          transform:translateX(-16px); }
  35%  { filter:brightness(3) drop-shadow(0 0 20px #ff0000);
          transform:translateX(12px); }
  60%  { filter:brightness(1.5); transform:translateX(-5px); }
  100% { filter:drop-shadow(0 6px 12px rgba(0,0,0,0.5));
          transform:translateX(0); }
}
.anim-shake { animation: shakeEl 0.5s ease-in-out !important; }
@keyframes shakeEl {
  0%,100% { transform:translate(0); }
  15%  { transform:translate(-8px, 4px);  }
  30%  { transform:translate( 8px,-4px);  }
  45%  { transform:translate(-6px, 2px);  }
  60%  { transform:translate( 6px,-2px);  }
  75%  { transform:translate(-3px, 0);    }
}

/* ── STATUS BAR ── */
#status-bar {
  display: flex; align-items: center;
  justify-content: space-between;
  padding: clamp(4px,0.8vh,7px) clamp(12px,3vw,20px);
  background: rgba(0,0,0,0.4);
  border-top: 1px solid rgba(255,255,255,0.04);
  border-bottom: 1px solid rgba(255,255,255,0.04);
  flex-shrink: 0;
}
#phase-label {
  font-family: 'Press Start 2P', monospace;
  font-size: clamp(0.38rem,1.2vw,0.54rem);
  color: var(--gold); letter-spacing: 1px;
}
#q-answered-wrap {
  font-family: 'Press Start 2P', monospace;
  font-size: clamp(0.3rem,0.9vw,0.44rem);
  color: var(--text-secondary);
}

/* ════════════════════════════════
   PLAY AREA
   ════════════════════════════════ */
#play-area {
  flex: 1; width: 100%;
  display: flex; flex-direction: column;
  overflow: hidden; min-height: 0;
}

/* ── CARD ROW ── */
#card-row {
  display: flex;
  gap: clamp(7px,1.8vw,16px);
  padding: clamp(10px,2vh,18px)
           clamp(12px,3vw,22px)
           clamp(8px,1.5vh,14px);
  flex-shrink: 0; justify-content: center;
}
.card-slot {
  flex: 1; max-width: 115px;
  height: var(--slot-h);
  background: linear-gradient(135deg,#1a1a50,#0d0d30,#1a0a30);
  border: 2px solid var(--card-border);
  border-radius: 14px; cursor: pointer;
  display: flex; align-items: center; justify-content: center;
  position: relative; overflow: hidden;
  transition: transform 0.2s, box-shadow 0.2s,
              border-color 0.2s, opacity 0.3s;
  -webkit-tap-highlight-color: transparent;
  box-shadow: 0 4px 18px rgba(0,0,0,0.4);
}
.card-slot::before {
  content: ''; position: absolute; inset: 0;
  background: linear-gradient(45deg,
    transparent 30%, rgba(255,255,255,0.04) 50%, transparent 70%);
  animation: cardShimmer 4s ease-in-out infinite;
}
@keyframes cardShimmer {
  0%   { transform: translateX(-100%); }
  100% { transform: translateX(200%);  }
}
.card-slot::after {
  content: ''; position: absolute; inset: 5px;
  border: 1px solid rgba(255,255,255,0.07);
  border-radius: 10px; pointer-events: none;
}
.slot-inner {
  display: flex; flex-direction: column;
  align-items: center; justify-content: center;
  gap: 4px; z-index: 1; pointer-events: none;
}
.slot-icon {
  font-size: clamp(1.5rem,5vw,2.4rem);
  animation: slotPulse 2.8s ease-in-out infinite;
  filter: drop-shadow(0 0 8px rgba(168,85,247,0.45));
}
@keyframes slotPulse {
  0%,100% { transform: scale(1);   opacity: 0.85; }
  50%     { transform: scale(1.1); opacity: 1;    }
}
.slot-dmg {
  font-family: 'Press Start 2P', monospace;
  font-size: clamp(0.3rem,0.95vw,0.44rem);
  color: var(--text-secondary); letter-spacing: 0.5px;
}
.card-slot:not(.used):not(.disabled):not(.selected):hover {
  transform: translateY(-5px) scale(1.04);
  border-color: var(--cyan);
  box-shadow: 0 8px 28px rgba(0,212,255,0.28);
}
.card-slot:not(.used):not(.disabled):active {
  transform: scale(0.97);
}
.card-slot.selected {
  border-color: var(--gold);
  box-shadow: 0 0 24px rgba(255,215,0,0.4);
  transform: translateY(-5px);
  animation: selectedPulse 1.2s ease-in-out infinite;
}
@keyframes selectedPulse {
  0%,100% { box-shadow: 0 0 18px rgba(255,215,0,0.3); }
  50%     { box-shadow: 0 0 34px rgba(255,215,0,0.65); }
}
.card-slot.correct-card {
  border-color: var(--correct) !important;
  box-shadow: 0 0 18px rgba(34,197,94,0.35) !important;
  animation: none !important;
}
.card-slot.wrong-card {
  border-color: var(--wrong) !important;
  box-shadow: 0 0 18px rgba(239,68,68,0.35) !important;
  animation: none !important;
}
.card-slot.used {
  opacity: 0.25; pointer-events: none; cursor: default;
}
.card-slot.disabled {
  pointer-events: none; cursor: default;
}

/* ── QUESTION PANEL ── */
#question-panel {
  flex: 1; min-height: 0;
  display: flex; flex-direction: column;
  margin: 0 clamp(10px,2.5vw,20px)
            clamp(8px,1.5vh,14px);
  background: linear-gradient(160deg,#0e0e2e,#131340);
  border: 2px solid var(--border);
  border-radius: 16px; overflow: hidden;
  animation: panelSlideUp 0.38s cubic-bezier(0.34,1.56,0.64,1);
  box-shadow: 0 4px 28px rgba(0,0,0,0.5),
              inset 0 1px 0 rgba(255,255,255,0.05);
}
@keyframes panelSlideUp {
  from { transform: translateY(28px); opacity: 0; }
  to   { transform: translateY(0);    opacity: 1; }
}
#qp-header {
  display: flex; align-items: center;
  justify-content: space-between;
  padding: clamp(7px,1.4vh,12px) clamp(12px,3vw,18px);
  background: rgba(0,0,0,0.3);
  border-bottom: 1px solid rgba(255,255,255,0.06);
  flex-shrink: 0; gap: 12px;
}
#qp-dmg-badge {
  font-family: 'Press Start 2P', monospace;
  font-size: clamp(0.44rem,1.4vw,0.62rem);
  color: #fff;
  background: linear-gradient(135deg,#cc2233,#ff5544);
  border-radius: 20px; padding: 4px 12px;
  letter-spacing: 0.5px;
  box-shadow: 0 2px 8px rgba(255,50,50,0.35);
  flex-shrink: 0;
}
#qp-timer-wrap {
  display: flex; align-items: center;
  gap: 8px; flex: 1; min-width: 0;
}
#qp-timer-bar {
  flex: 1; height: clamp(5px,0.9vh,9px);
  background: #0a0a1a; border-radius: 5px;
  overflow: hidden; border: 1px solid var(--border);
}
#qp-timer-fill {
  height: 100%; width: 100%;
  background: var(--hp-green); border-radius: 5px;
  transition: background 0.3s;
}
#qp-timer-text {
  font-family: 'Press Start 2P', monospace;
  font-size: clamp(0.32rem,1vw,0.48rem);
  color: var(--text-secondary);
  flex-shrink: 0; min-width: 26px; text-align: right;
}

/* ── Question body (scrollable) ── */
#qp-body {
  flex: 1; min-height: 0;
  display: flex; flex-direction: column;
  overflow: hidden;
}

/* Question content area */
#qp-question {
  flex-shrink: 0;
  max-height: var(--qp-body-maxh);
  overflow-y: auto;
  -webkit-overflow-scrolling: touch;
  display: flex;
  flex-direction: column;
  gap: clamp(5px,1vh,9px);
  padding: clamp(8px,1.5vh,14px)
           clamp(12px,3vw,20px)
           clamp(5px,1vh,9px);
}
#qp-question::-webkit-scrollbar { width: 4px; }
#qp-question::-webkit-scrollbar-track {
  background: rgba(255,255,255,0.03); border-radius: 4px;
}
#qp-question::-webkit-scrollbar-thumb {
  background: rgba(0,212,255,0.3); border-radius: 4px;
}

/* Background paragraph */
.q-background {
  font-size: clamp(0.72rem,2.1vw,0.86rem);
  color: var(--text-secondary);
  line-height: 1.65;
  background: rgba(255,255,255,0.03);
  border-left: 3px solid rgba(0,212,255,0.35);
  padding: clamp(5px,1vh,9px) clamp(8px,2vw,13px);
  border-radius: 0 8px 8px 0;
  font-style: italic;
}
.q-background .katex { color: var(--cyan); font-size: 1.1em; }

/* Bullet list */
.q-bullet-list {
  list-style: none; padding: 0; margin: 0;
  display: flex; flex-direction: column;
  gap: clamp(3px,0.6vh,5px);
}
.q-bullet-list li {
  font-size: clamp(0.7rem,2vw,0.83rem);
  color: var(--text-secondary); line-height: 1.5;
  padding: clamp(4px,0.7vh,7px) clamp(8px,2vw,12px);
  background: rgba(255,255,255,0.03);
  border-radius: 7px;
  border-left: 2px solid rgba(168,85,247,0.4);
  display: flex; align-items: flex-start; gap: 6px;
}
.q-bullet-list li::before {
  content: '▸'; color: var(--purple);
  flex-shrink: 0; margin-top: 1px;
}
.q-bullet-list li .katex { color: var(--cyan); }

/* Data table */
.q-table-wrap {
  width: 100%; overflow-x: auto;
  -webkit-overflow-scrolling: touch;
  border-radius: 10px;
  border: 1px solid rgba(255,255,255,0.08);
}
.q-table {
  width: 100%; border-collapse: collapse;
  font-size: clamp(0.6rem,1.8vw,0.76rem);
  min-width: 200px;
}
.q-table thead tr {
  background: linear-gradient(90deg,
    rgba(168,85,247,0.25), rgba(0,212,255,0.2));
}
.q-table th {
  padding: clamp(5px,1vh,9px) clamp(7px,1.6vw,12px);
  font-family: 'Press Start 2P', monospace;
  font-size: clamp(0.26rem,0.85vw,0.4rem);
  color: var(--cyan); text-align: center;
  letter-spacing: 0.3px;
  border-bottom: 1px solid rgba(255,255,255,0.1);
  white-space: nowrap;
}
.q-table td {
  padding: clamp(4px,0.8vh,8px) clamp(7px,1.6vw,12px);
  text-align: center; color: var(--text-primary);
  border-bottom: 1px solid rgba(255,255,255,0.05);
  line-height: 1.4;
}
.q-table .row-even td { background: rgba(255,255,255,0.02); }
.q-table .row-odd  td { background: rgba(0,212,255,0.04);   }
.q-table tbody tr:last-child td { border-bottom: none; }
.q-table td .katex { color: var(--cyan); font-size: 1.05em; }

/* Question stem */
.q-stem {
  font-size: clamp(0.8rem,2.4vw,0.94rem);
  font-weight: 700; color: var(--text-primary);
  line-height: 1.65;
  padding: clamp(6px,1.2vh,10px) clamp(8px,2vw,13px);
  background: rgba(255,215,0,0.06);
  border: 1px solid rgba(255,215,0,0.2);
  border-radius: 9px; text-align: left;
}
.q-stem .katex { color: var(--gold); font-size: 1.1em; }

/* Options */
#qp-options {
  display: flex; flex-direction: column;
  gap: clamp(5px,1vh,9px);
  padding: clamp(6px,1.2vh,10px)
           clamp(12px,3vw,20px)
           clamp(8px,1.5vh,14px);
  overflow-y: auto;
  -webkit-overflow-scrolling: touch;
  flex-shrink: 0;
}
#qp-options::-webkit-scrollbar { width: 4px; }
#qp-options::-webkit-scrollbar-track {
  background: rgba(255,255,255,0.03); border-radius: 4px;
}
#qp-options::-webkit-scrollbar-thumb {
  background: rgba(168,85,247,0.3); border-radius: 4px;
}
.qp-opt {
  padding: clamp(9px,1.7vh,14px) clamp(12px,2.5vw,18px);
  font-size: clamp(0.78rem,2.3vw,var(--qp-opt-fs));
  font-family: 'Chakra Petch', sans-serif;
  font-weight: 600;
  background: rgba(255,255,255,0.05);
  color: var(--text-primary);
  border: 2px solid rgba(255,255,255,0.1);
  border-radius: 11px; cursor: pointer;
  transition: all 0.15s;
  text-align: center; line-height: 1.55;
  min-height: var(--qp-opt-minh);
  display: flex; align-items: center; justify-content: center;
  -webkit-tap-highlight-color: transparent;
  flex-shrink: 0; word-break: break-word;
}
.qp-opt .katex { font-size: 1.15em; }
.qp-opt:hover:not(:disabled) {
  background: rgba(0,212,255,0.13);
  border-color: var(--cyan);
  transform: translateX(3px);
  box-shadow: 0 3px 14px rgba(0,212,255,0.15);
}
.qp-opt:active:not(:disabled) { transform: scale(0.98); }
.qp-opt.correct {
  background: rgba(34,197,94,0.2) !important;
  border-color: var(--correct) !important;
  color: var(--correct);
  box-shadow: 0 0 18px rgba(34,197,94,0.22);
}
.qp-opt.correct .katex { color: var(--correct); }
.qp-opt.wrong {
  background: rgba(239,68,68,0.18) !important;
  border-color: var(--wrong) !important;
  color: var(--wrong);
}
.qp-opt.wrong .katex { color: var(--wrong); }
.qp-opt:disabled { cursor: default; opacity: 0.72; }

/* ── BONUS PANEL ── */
#bonus-panel {
  flex: 1; min-height: 0;
  display: flex; flex-direction: column;
  align-items: center; justify-content: center;
  padding: clamp(12px,2.5vh,24px)
           clamp(14px,3.5vw,24px);
  gap: clamp(10px,2vh,18px);
  animation: panelSlideUp 0.38s cubic-bezier(0.34,1.56,0.64,1);
}
#bonus-title {
  font-family: 'Press Start 2P', monospace;
  font-size: clamp(0.72rem,2.4vw,1.05rem);
  color: var(--gold);
  text-shadow: 0 0 18px rgba(255,215,0,0.5);
  animation: titlePulse 1.5s ease-in-out infinite alternate;
  letter-spacing: 2px; text-align: center;
}
#bonus-subtitle {
  font-size: clamp(0.72rem,2.2vw,0.9rem);
  color: var(--text-secondary);
  font-style: italic; text-align: center;
}
#bonus-card-row {
  display: flex; gap: clamp(12px,3vw,24px);
  justify-content: center; width: 100%;
}
.bonus-slot {
  flex: 1; max-width: 150px;
  aspect-ratio: 3/4;
  max-height: clamp(140px,28vh,200px);
  background: linear-gradient(135deg,#2a1a50,#1a0a40);
  border: 2px solid var(--gold); border-radius: 16px;
  cursor: pointer;
  display: flex; align-items: center; justify-content: center;
  position: relative; overflow: hidden;
  transition: transform 0.2s, box-shadow 0.2s;
  -webkit-tap-highlight-color: transparent;
  box-shadow: 0 0 16px rgba(255,215,0,0.15);
}
.bonus-slot:not(.revealed):not(.disabled):hover {
  transform: translateY(-6px) scale(1.05);
  box-shadow: 0 10px 30px rgba(255,215,0,0.38);
}
.bonus-slot:not(.revealed):not(.disabled):active {
  transform: scale(0.97);
}
.bonus-slot.revealed { pointer-events: none; }
.bonus-slot.disabled { opacity: 0.38; pointer-events: none; }
.bonus-slot-inner {
  display: flex; flex-direction: column;
  align-items: center; justify-content: center;
  gap: 8px; z-index: 1; pointer-events: none;
  padding: 12px; text-align: center;
}
.bslot-icon {
  font-size: clamp(1.8rem,6vw,3.2rem);
  animation: slotPulse 2.2s ease-in-out infinite;
  filter: drop-shadow(0 0 10px rgba(255,215,0,0.45));
}
.bslot-label {
  font-family: 'Press Start 2P', monospace;
  font-size: clamp(0.42rem,1.4vw,0.6rem);
  color: var(--gold); letter-spacing: 1px;
  line-height: 1.6; white-space: pre-line;
}
#bonus-result {
  font-family: 'Press Start 2P', monospace;
  font-size: clamp(0.52rem,1.7vw,0.75rem);
  text-align: center; line-height: 1.9;
  padding: 10px 18px;
  background: rgba(0,0,0,0.3);
  border: 1px solid rgba(255,255,255,0.08);
  border-radius: 12px;
  animation: panelSlideUp 0.3s ease-out;
  max-width: 320px;
}

/* ════════════════════════════════
   END SCREEN
   ════════════════════════════════ */
#end-screen {
  overflow-y: auto;
  -webkit-overflow-scrolling: touch;
  justify-content: center;
  background:
    radial-gradient(ellipse at 50% 0%,
      rgba(255,215,0,0.07), transparent 60%),
    var(--bg-dark);
}
.end-box {
  text-align: center;
  padding: clamp(1.2rem,3.5vh,2.5rem)
           clamp(1.2rem,4vw,2rem);
  width: 92%; max-width: 460px; margin: auto;
}
#end-icon {
  font-size: clamp(3rem,10vw,5rem);
  margin-bottom: 0.6rem; display: block;
  animation: endBounce 2s ease-in-out infinite;
}
@keyframes endBounce {
  0%,100% { transform: scale(1);    }
  50%     { transform: scale(1.15); }
}
#end-title {
  font-family: 'Press Start 2P', monospace;
  font-size: clamp(0.72rem,2.8vw,1.15rem);
  margin-bottom: 0.7rem; line-height: 1.6;
  animation: endGlow 2s ease-in-out infinite alternate;
}
@keyframes endGlow {
  from { text-shadow: 0 0 5px  currentColor; }
  to   { text-shadow: 0 0 28px currentColor,
                      0 0 55px currentColor; }
}
#end-reason {
  color: var(--text-secondary);
  font-size: clamp(0.8rem,2.5vw,0.95rem);
  margin-bottom: clamp(0.8rem,2.5vh,1.5rem);
  line-height: 1.6;
}
#star-rating {
  font-size: clamp(1.6rem,5.5vw,2.2rem);
  margin-bottom: clamp(0.6rem,1.8vh,1.2rem);
  letter-spacing: 5px;
}
#badges-row {
  display: flex; flex-wrap: wrap; gap: 7px;
  justify-content: center;
  margin-bottom: clamp(0.9rem,2.2vh,1.6rem);
}
.badge {
  background: rgba(255,215,0,0.1);
  border: 1px solid rgba(255,215,0,0.35);
  border-radius: 18px; padding: 5px 13px;
  font-size: clamp(0.6rem,1.7vw,0.75rem);
  color: var(--gold); font-weight: 700;
}
.stats-grid {
  display: grid; grid-template-columns: repeat(3,1fr);
  gap: clamp(6px,1.4vw,12px);
  margin-bottom: clamp(0.9rem,2.5vh,1.8rem);
}
.stat-item {
  background: var(--bg-card);
  border: 1px solid var(--border);
  border-radius: 13px;
  padding: clamp(8px,1.6vh,14px) clamp(5px,1.2vw,10px);
}
.stat-label {
  font-size: clamp(0.28rem,0.95vw,0.42rem);
  font-family: 'Press Start 2P', monospace;
  color: var(--text-secondary);
  margin-bottom: clamp(4px,0.9vh,8px);
}
.stat-value {
  font-family: 'Press Start 2P', monospace;
  font-size: clamp(0.62rem,2.3vw,0.98rem);
  color: var(--gold);
}
#try-again-btn {
  background: linear-gradient(135deg,var(--cyan),#1d4ed8);
  color: #fff;
  box-shadow: 0 4px 22px rgba(0,212,255,0.3);
}
#try-again-btn:hover {
  transform: translateY(-2px) scale(1.02);
  box-shadow: 0 6px 34px rgba(0,212,255,0.5);
}

/* ════════════════════════════════
   RESPONSIVE
   ════════════════════════════════ */
@media (max-width:360px) {
  :root {
    --arena-h:110px; --slot-h:58px;
    --qp-q-fs:0.82rem; --qp-opt-fs:0.74rem;
    --qp-opt-minh:38px; --qp-body-maxh:clamp(140px,28vh,200px);
  }
  .login-title  { font-size:0.95rem; }
  .login-dragon { font-size:3rem; }
  .q-table { font-size:0.58rem; }
  .q-table th { font-size:0.24rem; padding:4px 5px; }
  .q-table td { padding:3px 5px; }
}

@media (max-height:620px) and (orientation:portrait) {
  :root {
    --arena-h:90px; --slot-h:56px;
    --qp-q-fs:0.82rem; --qp-opt-fs:0.72rem;
    --qp-opt-minh:36px; --qp-body-maxh:clamp(120px,24vh,180px);
  }
  #boss-sprite   { font-size:1.7rem; }
  #player-sprite { font-size:1.5rem; }
  #boss-health-ring { width:58px; height:58px; }
}

@media (orientation:landscape) and (max-height:500px) {
  :root {
    --arena-h:0px; --slot-h:52px;
    --qp-q-fs:0.78rem; --qp-opt-fs:0.68rem;
    --qp-opt-minh:32px; --qp-body-maxh:clamp(80px,18vh,130px);
  }
  #arena         { display:none; }
  #play-area     { flex-direction:row; }
  #card-row {
    flex-direction:column; width:78px;
    padding:8px 5px; gap:5px; flex-shrink:0;
  }
  .card-slot     { width:100%; max-width:none; height:auto; flex:1; }
  #question-panel { flex:1; margin:5px 6px 5px 2px; }
  #bonus-panel   { flex:1; }
  #bonus-card-row { flex-direction:row; }
  .bonus-slot    { max-height:110px; }
}

@media (min-width:600px) and (max-width:1023px) {
  :root {
    --arena-h:175px; --slot-h:90px;
    --qp-q-fs:1.05rem; --qp-opt-fs:0.94rem;
    --qp-opt-minh:50px; --qp-body-maxh:clamp(200px,36vh,320px);
  }
  #game-container { max-width:640px; }
  #boss-sprite    { font-size:3.5rem; }
  #player-sprite  { font-size:3rem;   }
  #boss-health-ring { width:95px; height:95px; }
  .card-slot      { max-width:135px; }
  .q-table        { font-size:0.8rem; }
  .q-table th     { font-size:0.42rem; }
}

@media (min-width:1024px) {
  :root {
    --arena-h:195px; --slot-h:96px;
    --qp-q-fs:1.1rem; --qp-opt-fs:0.96rem;
    --qp-opt-minh:50px; --qp-body-maxh:clamp(220px,38vh,340px);
  }
  #game-container {
    max-width:520px;
    height:min(100dvh,920px);
    border-radius:24px;
    box-shadow:
      0 0 0 1px rgba(255,255,255,0.06),
      0 30px 80px rgba(0,0,0,0.8);
  }
  #boss-sprite    { font-size:3.8rem; }
  #player-sprite  { font-size:3.2rem; }
  #boss-health-ring { width:105px; height:105px; }
  .card-slot      { max-width:118px; }
  .card-slot:not(.used):not(.disabled):not(.selected):hover {
    transform:translateY(-6px) scale(1.05);
  }
  .q-table        { font-size:0.82rem; }
  .q-table th     { font-size:0.44rem; }
}